{
    "services": {
        "backend": {
            "root": "backend",
            "entrypoint": "server.js",
            "buildCommand": "cd ../frontend && npm install && npm run build && cp -r dist ../backend/dist"
        }
    },
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": {
                "type": "service",
                "service": "backend"
            }
        }
    ]
}
